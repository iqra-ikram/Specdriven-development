# Plan: Author Book Content

**Feature**: 002-author-book-content
**Goal**: Implement the "Physical AI & Humanoid Robotics" book content.

## 1. File Structure Strategy

We will organize the documentation in `docusaurus-root/docs/` as follows:

```
docs/
├── intro.md                  # Introduction, Why Physical AI, Learning Outcomes
├── schedule.md               # Weekly Breakdown
├── hardware.md               # Hardware Requirements & Lab Options
├── assessments.md            # Assessments & Capstone
└── modules/                  # Folder for core modules
    ├── 01-nervous-system.md  # Module 1: ROS 2
    ├── 02-digital-twin.md    # Module 2: Gazebo & Unity
    ├── 03-ai-robot-brain.md  # Module 3: NVIDIA Isaac
    └── 04-vla.md             # Module 4: Vision-Language-Action
```

## 2. Sidebar Configuration (`sidebars.ts`)

We will configure the sidebar to be autogenerated or manually specified to ensure the correct order:
1. Introduction
2. Schedule
3. Modules (Dropdown)
4. Hardware Requirements
5. Assessments

## 3. Content Implementation Details

### 3.1. Introduction (`intro.md`)
- **Frontmatter**: `sidebar_position: 1`
- **Components**:
  - Hero text block.
  - "Why It Matters" section with an info admonition.
  - Learning Outcomes list.

### 3.2. Schedule (`schedule.md`)
- **Frontmatter**: `sidebar_position: 2`
- **Components**:
  - Timeline or Table view of Weeks 1-13.

### 3.3. Modules (`modules/*.md`)
- **Frontmatter**: `sidebar_position: 3` (Group)
- **Structure**:
  - Overview.
  - Key Topics (ROS Nodes, Physics, Isaac Sim, etc.).
  - Placeholder for architecture diagrams.

### 3.4. Hardware (`hardware.md`)
- **Frontmatter**: `sidebar_position: 4`
- **Components**:
  - Tabs for "Workstation" vs "Edge Kit" vs "Lab Options" (using Docusaurus Tabs).
  - Warning admonitions for "Latency Trap" and "GPU Bottleneck".

### 3.5. Assessments (`assessments.md`)
- **Frontmatter**: `sidebar_position: 5`
- **Components**:
  - List of projects.
  - Capstone details.

## 4. Execution Steps
1. **Clean up**: Remove default `docs/tutorial-*` files and folders.
2. **Create structure**: Create `docs/modules/` directory.
3. **Write content**: Create the MDX files with the content mapped from the spec.
4. **Update Sidebar**: Ensure `sidebars.ts` is set to autogenerate or explicitly defined.
5. **Assets**: Add a placeholder image for the "Architecture Summary" in `static/img/`.
6. **Verify**: Build the site.
