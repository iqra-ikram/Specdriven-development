[project]
name = "backend"
version = "0.1.0"
description = ""
authors = [
    {name = "DevAbdullah90",email = "abdullahkashif12b3@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi (>=0.123.8,<0.124.0)",
    "uvicorn (>=0.38.0,<0.39.0)",
    "pydantic (>=2.12.5,<3.0.0)",
    "httpx (>=0.28.1,<0.29.0)",
    "python-dotenv (>=1.2.1,<2.0.0)",
    "qdrant-client (>=1.16.1,<2.0.0)",
    "psycopg2-binary (>=2.9.11,<3.0.0)",
    "google-generativeai (>=0.8.0,<0.9.0)",
    "sqlalchemy (>=2.0.44,<3.0.0)",
    "fastembed (>=0.4.2,<1.0.0)",
    "deep-translator (>=1.0.0)"
]

[tool.poetry]
packages = [{include = "src"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "pytest (>=9.0.1,<10.0.0)",
    "pytest-httpx (>=0.36.0,<0.37.0)",
    "pytest-asyncio (>=1.3.0,<2.0.0)"
]

[tool.pytest.ini_options]
pythonpath = ["src"]
